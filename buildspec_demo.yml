version: 0.2
            
phases:
  install:
    commands:
      - echo Installing grow
      - apt-get update -y 
      - apt-get install -y python-pip
      - pip install grow
  build:
    commands:
      - grow build
  post_build:
    commands:
      - aws s3 sync --delete build/ "s3://nd-website-demo"