name: Slack alert
on:
  issues:
    types: [closed]
jobs:
  deploy:
    if: contains(github.event.issue.labels.*.name, 'CDO :fire:')
    runs-on: ubuntu-latest
    steps:
    - name: Send Message
      run: |
        curl -X POST -H 'Content-type: application/json' --data '{"text":"CDO Issue on Github was just closed :tada:\nTitle: ${{ github.event.issue.title }}\nIssue Number: <${{ github.event.issue.html_url }}|#${{ github.event.issue.number }}>"}' ${{ secrets.JMS_KEY }}
        curl -X POST -H 'Content-type: application/json' --data '{"text":"CDO Issue on Github was just closed :tada:\nTitle: ${{ github.event.issue.title }}\nIssue Number: <${{ github.event.issue.html_url }}|#${{ github.event.issue.number }}>"}' ${{ secrets.PI_KEY }}
