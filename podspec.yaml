grow_version: ">=0.0.60"

localization:
  default_locale: en_US
  locales:
  - de_DE
  - en_US
  - ja_JP

static_dirs:
- static_dir: /source/images/
  serve_at: /static/images/
- static_dir: /dist/
  serve_at: /static/
  
#preprocessors:
#- kind: gulp
